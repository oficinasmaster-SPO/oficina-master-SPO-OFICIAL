{
  "name": "TenantMembership",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "ID do User vinculado (obrigatório)"
    },
    "workshop_id": {
      "type": "string",
      "description": "ID do Workshop (tenant) ao qual o usuário tem acesso (obrigatório)"
    },
    "company_id": {
      "type": "string",
      "description": "ID da empresa matriz (quando o workshop pertence a uma rede)"
    },
    "consulting_firm_id": {
      "type": "string",
      "description": "ID do escritório de consultoria que gerencia este tenant"
    },
    "employee_id": {
      "type": "string",
      "description": "ID do Employee correspondente neste workshop (quando existir)"
    },
    "profile_id": {
      "type": "string",
      "description": "ID do UserProfile (perfil de acesso) aplicado nesta membership"
    },
    "membership_type": {
      "type": "string",
      "enum": [
        "employee",
        "owner",
        "partner",
        "consultant",
        "admin_support"
      ],
      "description": "Tipo de vínculo do usuário com o tenant"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "pending",
        "suspended",
        "revoked"
      ],
      "default": "active",
      "description": "Status da membership"
    },
    "is_default": {
      "type": "boolean",
      "default": false,
      "description": "Se é a membership padrão do usuário (tenant carregado no login). Donos de rede têm uma membership por filial e alternam entre elas."
    },
    "notes": {
      "type": "string",
      "description": "Observações/origem da membership (ex: backfill-user-field, backfill-owner)"
    }
  },
  "required": [
    "user_id",
    "workshop_id"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "user_type": "internal"
          }
        },
        {
          "user_condition": {
            "data": {
              "user_type": "internal"
            }
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "$and": [
            {
              "data.consulting_firm_id": "{{user.consulting_firm_id}}"
            },
            {
              "user_condition": {
                "user_type": "internal"
              }
            }
          ]
        },
        {
          "$and": [
            {
              "data.consulting_firm_id": "{{user.data.consulting_firm_id}}"
            },
            {
              "user_condition": {
                "data.user_type": "internal"
              }
            }
          ]
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "user_type": "internal"
          }
        },
        {
          "user_condition": {
            "data": {
              "user_type": "internal"
            }
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "user_type": "internal"
          }
        },
        {
          "user_condition": {
            "data": {
              "user_type": "internal"
            }
          }
        }
      ]
    }
  }
}
